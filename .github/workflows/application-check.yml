name: Application Check

on:
  pull_request:

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: ブランチ名
        run: |
          echo ${{ github.head_ref }}
          echo ${{ github.base_ref }}
      - name: local->staging のみskip
        run: exit 0
        if: ${{ github.head_ref == 'local' && github.base_ref == 'staging' }}
      - name: Hello
        run: echo "World"
